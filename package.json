{
  "name": "skyflow-agent-demo",
  "version": "1.0.0",
  "description": "Demo showcasing Skyflow's effectiveness in protecting AI agents from data exfiltration attacks",
  "main": "dist/runner.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/runner.js",
    "demo": "tsc && node dist/runner.js",
    "setup-vault": "tsc && node dist/setup-vault.js",
    "dashboard": "cd dashboard && npm run dev",
    "dashboard:build": "cd dashboard && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "skyflow-node": "^1.13.0",
    "chalk": "^4.1.2",
    "dotenv": "^16.4.5",
    "redisvl": "^0.1.4",
    "redis": "^4.7.0",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
